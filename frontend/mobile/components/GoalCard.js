import React from 'react';
import { TouchableOpacity, View, Text, StyleSheet } from 'react-native';
import { COLORS, FONTS } from '../theme';
export default function GoalCard({ goal, selected, onPress }) { return <TouchableOpacity activeOpacity={.8} onPress={onPress} style={[styles.card, selected && styles.active]}><Text style={styles.emoji}>{goal.icon}</Text><View style={styles.copy}><Text style={styles.title}>{goal.title}</Text><Text style={styles.description}>{goal.description}</Text></View><View style={[styles.check, selected && styles.checked]}><Text style={styles.mark}>{selected ? '✓' : ''}</Text></View></TouchableOpacity>; }
const styles = StyleSheet.create({ card: { flexDirection: 'row', alignItems: 'center', backgroundColor: '#fff', borderWidth: 1, borderColor: '#d7e5db', borderRadius: 16, padding: 14, marginBottom: 10, elevation: 1 }, active: { borderColor: COLORS.primary, backgroundColor: '#f0fdf4' }, emoji: { fontSize: 23, marginRight: 11 }, copy: { flex: 1 }, title: { fontFamily: FONTS.bold, color: '#173d28', fontSize: 15, marginBottom: 3 }, description: { fontFamily: FONTS.regular, color: COLORS.mutedText, fontSize: 11, lineHeight: 16 }, check: { width: 22, height: 22, borderRadius: 11, borderWidth: 1.5, borderColor: COLORS.inputBorder, alignItems: 'center', justifyContent: 'center', marginLeft: 8 }, checked: { backgroundColor: COLORS.primary, borderColor: COLORS.primary }, mark: { color: '#fff', fontWeight: '800' } });
